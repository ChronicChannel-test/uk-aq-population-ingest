[project]
name = "nomis-api"
version = "0.1.0"
description = "Nomis population dataset ingestion"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.31.0",
  "openpyxl>=3.1.2",
]

[tool.ruff]
line-length = 100
